<Window x:Class="GhostLang.WPF.Views.SubtitleOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        SizeToContent="WidthAndHeight"
        Focusable="False"
        IsHitTestVisible="False">
    <Border Background="#CC000000" CornerRadius="8" Padding="22,14">
        <StackPanel>
            <TextBlock x:Name="OriginalText"
                       Foreground="#B0B0B0" FontSize="14"
                       TextWrapping="Wrap" MaxWidth="900"
                       HorizontalAlignment="Center"
                       TextAlignment="Center"
                       Margin="0,0,0,4" />
            <TextBlock x:Name="TranslatedText"
                       Foreground="White" FontSize="22" FontWeight="SemiBold"
                       TextWrapping="Wrap" MaxWidth="900"
                       HorizontalAlignment="Center"
                       TextAlignment="Center" />
        </StackPanel>
    </Border>
</Window>